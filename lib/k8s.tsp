// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "k8s" attribute keys (string constants matching the upstream YAML). */
namespace OTel.Keys.K8s;

/**
 * The name of the cluster.
 * @stability release_candidate
 */
alias ClusterName = "k8s.cluster.name";

/**
 * A pseudo-ID for the cluster, set to the UID of the `kube-system` namespace.
 * @stability release_candidate
 */
alias ClusterUid = "k8s.cluster.uid";

/**
 * The name of the Container from Pod specification, must be unique within a Pod. Container runtime usually uses different globally unique name (`container.name`).
 * @stability release_candidate
 */
alias ContainerName = "k8s.container.name";

/**
 * Number of times the container was restarted. This attribute can be used to identify a particular container (running or stopped) within a container spec.
 * @stability release_candidate
 */
alias ContainerRestartCount = "k8s.container.restart_count";

/**
 * Last terminated reason of the Container.
 * @stability development
 */
alias ContainerStatusLastTerminatedReason = "k8s.container.status.last_terminated_reason";

/** The reason for the container state. Corresponds to the `reason` field of the: [K8s ContainerStateWaiting](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.30/#containerstatewaiting-v1-core) or [K8s ContainerStateTerminated](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.30/#containerstateterminated-v1-core) */
alias ContainerStatusReason = "k8s.container.status.reason";

/** The state of the container. [K8s ContainerState](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.30/#containerstate-v1-core) */
alias ContainerStatusState = "k8s.container.status.state";

/**
 * The cronjob annotation placed on the CronJob, the `<key>` being the annotation name, the value being the annotation value.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias CronjobAnnotation = "k8s.cronjob.annotation";

/**
 * The label placed on the CronJob, the `<key>` being the label name, the value being the label value.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias CronjobLabel = "k8s.cronjob.label";

/**
 * The name of the CronJob.
 * @stability release_candidate
 */
alias CronjobName = "k8s.cronjob.name";

/**
 * The UID of the CronJob.
 * @stability release_candidate
 */
alias CronjobUid = "k8s.cronjob.uid";

/**
 * The annotation placed on the DaemonSet, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias DaemonsetAnnotation = "k8s.daemonset.annotation";

/**
 * The label placed on the DaemonSet, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias DaemonsetLabel = "k8s.daemonset.label";

/**
 * The name of the DaemonSet.
 * @stability release_candidate
 */
alias DaemonsetName = "k8s.daemonset.name";

/**
 * The UID of the DaemonSet.
 * @stability release_candidate
 */
alias DaemonsetUid = "k8s.daemonset.uid";

/**
 * The annotation placed on the Deployment, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias DeploymentAnnotation = "k8s.deployment.annotation";

/**
 * The label placed on the Deployment, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias DeploymentLabel = "k8s.deployment.label";

/**
 * The name of the Deployment.
 * @stability release_candidate
 */
alias DeploymentName = "k8s.deployment.name";

/**
 * The UID of the Deployment.
 * @stability release_candidate
 */
alias DeploymentUid = "k8s.deployment.uid";

/**
 * The type of metric source for the horizontal pod autoscaler.
 * @stability development
 */
alias HpaMetricType = "k8s.hpa.metric.type";

/**
 * The name of the horizontal pod autoscaler.
 * @stability development
 */
alias HpaName = "k8s.hpa.name";

/**
 * The API version of the target resource to scale for the HorizontalPodAutoscaler.
 * @stability development
 */
alias HpaScaletargetrefApiVersion = "k8s.hpa.scaletargetref.api_version";

/**
 * The kind of the target resource to scale for the HorizontalPodAutoscaler.
 * @stability development
 */
alias HpaScaletargetrefKind = "k8s.hpa.scaletargetref.kind";

/**
 * The name of the target resource to scale for the HorizontalPodAutoscaler.
 * @stability development
 */
alias HpaScaletargetrefName = "k8s.hpa.scaletargetref.name";

/**
 * The UID of the horizontal pod autoscaler.
 * @stability development
 */
alias HpaUid = "k8s.hpa.uid";

/**
 * The size (identifier) of the K8s huge page.
 * @stability development
 */
alias HugepageSize = "k8s.hugepage.size";

/**
 * The annotation placed on the Job, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias JobAnnotation = "k8s.job.annotation";

/**
 * The label placed on the Job, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias JobLabel = "k8s.job.label";

/**
 * The name of the Job.
 * @stability release_candidate
 */
alias JobName = "k8s.job.name";

/**
 * The UID of the Job.
 * @stability release_candidate
 */
alias JobUid = "k8s.job.uid";

/**
 * The annotation placed on the Namespace, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias NamespaceAnnotation = "k8s.namespace.annotation";

/**
 * The label placed on the Namespace, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias NamespaceLabel = "k8s.namespace.label";

/**
 * The name of the namespace that the pod is running in.
 * @stability release_candidate
 */
alias NamespaceName = "k8s.namespace.name";

/**
 * The phase of the K8s namespace.
 * @stability development
 */
alias NamespacePhase = "k8s.namespace.phase";

/**
 * The annotation placed on the Node, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias NodeAnnotation = "k8s.node.annotation";

/**
 * The status of the condition, one of True, False, Unknown.
 * @stability development
 */
alias NodeConditionStatus = "k8s.node.condition.status";

/**
 * The condition type of a K8s Node.
 * @stability development
 */
alias NodeConditionType = "k8s.node.condition.type";

/**
 * The label placed on the Node, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias NodeLabel = "k8s.node.label";

/**
 * The name of the Node.
 * @stability release_candidate
 */
alias NodeName = "k8s.node.name";

/**
 * The name of the system container running on the K8s Node.
 * @stability development
 */
alias NodeSystemContainerName = "k8s.node.system_container.name";

/**
 * The UID of the Node.
 * @stability release_candidate
 */
alias NodeUid = "k8s.node.uid";

/**
 * The annotation placed on the PersistentVolume, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PersistentvolumeAnnotation = "k8s.persistentvolume.annotation";

/**
 * The label placed on the PersistentVolume, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PersistentvolumeLabel = "k8s.persistentvolume.label";

/**
 * The name of the PersistentVolume.
 * @stability development
 */
alias PersistentvolumeName = "k8s.persistentvolume.name";

/**
 * The reclaim policy of the PersistentVolume.
 * @stability development
 */
alias PersistentvolumeReclaimPolicy = "k8s.persistentvolume.reclaim_policy";

/**
 * The phase of the PersistentVolume.
 * @stability development
 */
alias PersistentvolumeStatusPhase = "k8s.persistentvolume.status.phase";

/**
 * The UID of the PersistentVolume.
 * @stability development
 */
alias PersistentvolumeUid = "k8s.persistentvolume.uid";

/**
 * The annotation placed on the PersistentVolumeClaim, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PersistentvolumeclaimAnnotation = "k8s.persistentvolumeclaim.annotation";

/**
 * The label placed on the PersistentVolumeClaim, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PersistentvolumeclaimLabel = "k8s.persistentvolumeclaim.label";

/**
 * The name of the PersistentVolumeClaim.
 * @stability development
 */
alias PersistentvolumeclaimName = "k8s.persistentvolumeclaim.name";

/**
 * The phase of the PersistentVolumeClaim.
 * @stability development
 */
alias PersistentvolumeclaimStatusPhase = "k8s.persistentvolumeclaim.status.phase";

/**
 * The UID of the PersistentVolumeClaim.
 * @stability development
 */
alias PersistentvolumeclaimUid = "k8s.persistentvolumeclaim.uid";

/**
 * The annotation placed on the Pod, the `<key>` being the annotation name, the value being the annotation value.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PodAnnotation = "k8s.pod.annotation";

/**
 * Specifies the hostname of the Pod.
 * @stability release_candidate
 */
alias PodHostname = "k8s.pod.hostname";

/**
 * IP address allocated to the Pod.
 * @stability release_candidate
 */
alias PodIp = "k8s.pod.ip";

/**
 * The label placed on the Pod, the `<key>` being the label name, the value being the label value.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias PodLabel = "k8s.pod.label";

/**
 * Deprecated, use `k8s.pod.label` instead.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 * @deprecated renamed → k8s.pod.label
 */
#deprecated "renamed → k8s.pod.label"
alias PodLabels = "k8s.pod.labels";

/**
 * The name of the Pod.
 * @stability release_candidate
 */
alias PodName = "k8s.pod.name";

/**
 * The start timestamp of the Pod.
 * @stability release_candidate
 */
alias PodStartTime = "k8s.pod.start_time";

/**
 * The phase for the pod. Corresponds to the `phase` field of the: [K8s PodStatus](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.33/#podstatus-v1-core)
 * @stability development
 */
alias PodStatusPhase = "k8s.pod.status.phase";

/**
 * The reason for the pod state. Corresponds to the `reason` field of the: [K8s PodStatus](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.33/#podstatus-v1-core)
 * @stability development
 */
alias PodStatusReason = "k8s.pod.status.reason";

/**
 * The UID of the Pod.
 * @stability release_candidate
 */
alias PodUid = "k8s.pod.uid";

/**
 * The annotation placed on the ReplicaSet, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias ReplicasetAnnotation = "k8s.replicaset.annotation";

/**
 * The label placed on the ReplicaSet, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias ReplicasetLabel = "k8s.replicaset.label";

/**
 * The name of the ReplicaSet.
 * @stability release_candidate
 */
alias ReplicasetName = "k8s.replicaset.name";

/**
 * The UID of the ReplicaSet.
 * @stability release_candidate
 */
alias ReplicasetUid = "k8s.replicaset.uid";

/**
 * The name of the replication controller.
 * @stability development
 */
alias ReplicationcontrollerName = "k8s.replicationcontroller.name";

/**
 * The UID of the replication controller.
 * @stability development
 */
alias ReplicationcontrollerUid = "k8s.replicationcontroller.uid";

/**
 * The name of the resource quota.
 * @stability development
 */
alias ResourcequotaName = "k8s.resourcequota.name";

/**
 * The name of the K8s resource a resource quota defines.
 * @stability development
 */
alias ResourcequotaResourceName = "k8s.resourcequota.resource_name";

/**
 * The UID of the resource quota.
 * @stability development
 */
alias ResourcequotaUid = "k8s.resourcequota.uid";

/**
 * The annotation placed on the Service, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias ServiceAnnotation = "k8s.service.annotation";

/**
 * The address type of the service endpoint.
 * @stability development
 */
alias ServiceEndpointAddressType = "k8s.service.endpoint.address_type";

/**
 * The condition of the service endpoint.
 * @stability development
 */
alias ServiceEndpointCondition = "k8s.service.endpoint.condition";

/**
 * The zone of the service endpoint.
 * @stability development
 */
alias ServiceEndpointZone = "k8s.service.endpoint.zone";

/**
 * The label placed on the Service, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias ServiceLabel = "k8s.service.label";

/**
 * The name of the Service.
 * @stability development
 */
alias ServiceName = "k8s.service.name";

/**
 * Whether the Service publishes not-ready endpoints.
 * @stability development
 */
alias ServicePublishNotReadyAddresses = "k8s.service.publish_not_ready_addresses";

/**
 * The selector key-value pair placed on the Service, the `<key>` being the selector key, the value being the selector value.
 * @stability development
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias ServiceSelector = "k8s.service.selector";

/**
 * The traffic distribution policy for the Service.
 * @stability development
 */
alias ServiceTrafficDistribution = "k8s.service.traffic_distribution";

/**
 * The type of the Kubernetes Service.
 * @stability development
 */
alias ServiceType = "k8s.service.type";

/**
 * The UID of the Service.
 * @stability development
 */
alias ServiceUid = "k8s.service.uid";

/**
 * The annotation placed on the StatefulSet, the `<key>` being the annotation name, the value being the annotation value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias StatefulsetAnnotation = "k8s.statefulset.annotation";

/**
 * The label placed on the StatefulSet, the `<key>` being the label name, the value being the label value, even if the value is empty.
 * @stability release_candidate
 * @remarks template attribute (prefix; append <key> at runtime)
 */
alias StatefulsetLabel = "k8s.statefulset.label";

/**
 * The name of the StatefulSet.
 * @stability release_candidate
 */
alias StatefulsetName = "k8s.statefulset.name";

/**
 * The UID of the StatefulSet.
 * @stability release_candidate
 */
alias StatefulsetUid = "k8s.statefulset.uid";

/**
 * The name of K8s [StorageClass](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.30/#storageclass-v1-storage-k8s-io) object.
 * @stability development
 */
alias StorageclassName = "k8s.storageclass.name";

/**
 * The name of the K8s volume.
 * @stability development
 */
alias VolumeName = "k8s.volume.name";

/**
 * The type of the K8s volume.
 * @stability development
 */
alias VolumeType = "k8s.volume.type";
