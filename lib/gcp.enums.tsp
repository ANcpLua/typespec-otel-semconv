// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "gcp" enum-typed attribute values. */
namespace OTel.Enums.Gcp;

/** Criticality of a destination workload indicates its importance to the business as specified in [AppHub type enum](https://cloud.google.com/app-hub/docs/reference/rest/v1/Attributes#type) */
enum GcpApphubDestinationServiceCriticalityType {
  /** Mission critical service. */
  MissionCritical: "MISSION_CRITICAL",
  /** High impact. */
  High: "HIGH",
  /** Medium impact. */
  Medium: "MEDIUM",
  /** Low impact. */
  Low: "LOW",
}

/** Software lifecycle stage of a destination service as defined [AppHub environment type](https://cloud.google.com/app-hub/docs/reference/rest/v1/Attributes#type_1) */
enum GcpApphubDestinationServiceEnvironmentType {
  /** Production environment. */
  Production: "PRODUCTION",
  /** Staging environment. */
  Staging: "STAGING",
  /** Test environment. */
  Test: "TEST",
  /** Development environment. */
  Development: "DEVELOPMENT",
}

/** Criticality of a destination workload indicates its importance to the business as specified in [AppHub type enum](https://cloud.google.com/app-hub/docs/reference/rest/v1/Attributes#type) */
enum GcpApphubDestinationWorkloadCriticalityType {
  /** Mission critical service. */
  MissionCritical: "MISSION_CRITICAL",
  /** High impact. */
  High: "HIGH",
  /** Medium impact. */
  Medium: "MEDIUM",
  /** Low impact. */
  Low: "LOW",
}

/** Environment of a destination workload is the stage of a software lifecycle as provided in the [AppHub environment type](https://cloud.google.com/app-hub/docs/reference/rest/v1/Attributes#type_1) */
enum GcpApphubDestinationWorkloadEnvironmentType {
  /** Production environment. */
  Production: "PRODUCTION",
  /** Staging environment. */
  Staging: "STAGING",
  /** Test environment. */
  Test: "TEST",
  /** Development environment. */
  Development: "DEVELOPMENT",
}

/** Criticality of a service indicates its importance to the business. */
enum GcpApphubServiceCriticalityType {
  /** Mission critical service. */
  MissionCritical: "MISSION_CRITICAL",
  /** High impact. */
  High: "HIGH",
  /** Medium impact. */
  Medium: "MEDIUM",
  /** Low impact. */
  Low: "LOW",
}

/** Environment of a service is the stage of a software lifecycle. */
enum GcpApphubServiceEnvironmentType {
  /** Production environment. */
  Production: "PRODUCTION",
  /** Staging environment. */
  Staging: "STAGING",
  /** Test environment. */
  Test: "TEST",
  /** Development environment. */
  Development: "DEVELOPMENT",
}

/** Criticality of a workload indicates its importance to the business. */
enum GcpApphubWorkloadCriticalityType {
  /** Mission critical service. */
  MissionCritical: "MISSION_CRITICAL",
  /** High impact. */
  High: "HIGH",
  /** Medium impact. */
  Medium: "MEDIUM",
  /** Low impact. */
  Low: "LOW",
}

/** Environment of a workload is the stage of a software lifecycle. */
enum GcpApphubWorkloadEnvironmentType {
  /** Production environment. */
  Production: "PRODUCTION",
  /** Staging environment. */
  Staging: "STAGING",
  /** Test environment. */
  Test: "TEST",
  /** Development environment. */
  Development: "DEVELOPMENT",
}
