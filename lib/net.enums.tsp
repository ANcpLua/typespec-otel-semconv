// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "net" enum-typed attribute values. */
namespace OTel.Enums.Net;

/**
 * Deprecated, use `network.transport` and `network.type`.
 * @deprecated uncategorized: Split to `network.transport` and `network.type`.
 */
#deprecated "uncategorized: Split to `network.transport` and `network.type`."
enum NetSockFamily {
  /** IPv4 address */
  Inet: "inet",
  /** IPv6 address */
  Inet6: "inet6",
  /** Unix domain socket path */
  Unix: "unix",
}

/**
 * Deprecated, use `network.transport`.
 * @deprecated renamed → network.transport
 */
#deprecated "renamed → network.transport"
enum NetTransport {
  IpTcp: "ip_tcp",
  IpUdp: "ip_udp",
  /** Named or anonymous pipe. */
  Pipe: "pipe",
  /** In-process communication. */
  Inproc: "inproc",
  /** Something else (non IP-based). */
  Other: "other",
}
