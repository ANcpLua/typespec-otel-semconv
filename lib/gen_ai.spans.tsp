// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "gen_ai" Spans. */
namespace OTel.Spans.GenAi;

/**
 * Describes GenAI agent creation and is usually applicable when working with remote agent services.
 * @stability development
 * @spanKind client
 */
alias CreateAgentClientSpan = "gen_ai.create_agent.client";

/**
 * Describes GenAI embeddings span - a request to a Generative AI model or service that generates an embeddings based on the input. The `gen_ai.operation.name` SHOULD be `embeddings`. **Span name** SHOULD be `{gen_ai.operation.name} {gen_ai.request.model}`.
 * @stability development
 * @spanKind client
 */
alias EmbeddingsClientSpan = "gen_ai.embeddings.client";

/**
 * Describes tool execution span.
 * @stability development
 * @spanKind internal
 */
alias ExecuteToolInternalSpan = "gen_ai.execute_tool.internal";

/**
 * This span represents a client call to Generative AI model or service that generates a response or requests a tool call based on the input prompt.
 * @stability development
 * @spanKind client
 */
alias InferenceClientSpan = "gen_ai.inference.client";

/**
 * Describes GenAI agent invocation over a remote service.
 * @stability development
 * @spanKind client
 */
alias InvokeAgentClientSpan = "gen_ai.invoke_agent.client";

/**
 * Describes GenAI agent invocation within the same process.
 * @stability development
 * @spanKind internal
 */
alias InvokeAgentInternalSpan = "gen_ai.invoke_agent.internal";

/**
 * Represents an operation that executes a coordinated process composed of multiple agents or other operations involving generative AI.
 * @stability development
 * @spanKind internal
 */
alias InvokeWorkflowInternalSpan = "gen_ai.invoke_workflow.internal";

/**
 * Describes GenAI retrieval span - a request to a Generative AI service or framework that retrieves relevant information or context from a vector database or search system. The `gen_ai.operation.name` SHOULD be `retrieval`. **Span name** SHOULD be `{gen_ai.operation.name} {gen_ai.data_source.id}`. Semantic conventions for individual GenAI providers and retrievers MAY specify different span name format.
 * @stability development
 * @spanKind client
 */
alias RetrievalClientSpan = "gen_ai.retrieval.client";
