// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "exception" attribute keys (string constants matching the upstream YAML). */
namespace OTel.Keys.Exception;

/**
 * Indicates that the exception is escaping the scope of the span.
 * @stability stable
 * @deprecated obsoleted: It's no longer recommended to record exceptions that are handled and do not escape the scope of a span.
 */
#deprecated "obsoleted: It's no longer recommended to record exceptions that are handled and do not escape the scope of a span."
alias Escaped = "exception.escaped";

/**
 * The exception message.
 * @stability stable
 */
alias Message = "exception.message";

/**
 * A stacktrace as a string in the natural representation for the language runtime. The representation is to be determined and documented by each language SIG.
 * @stability stable
 */
alias Stacktrace = "exception.stacktrace";

/**
 * The type of the exception (its fully-qualified class name, if applicable). The dynamic type of the exception should be preferred over the static type in languages that support it.
 * @stability stable
 */
alias Type = "exception.type";
