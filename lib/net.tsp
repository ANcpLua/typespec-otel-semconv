// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "net" attribute keys (string constants matching the upstream YAML). */
namespace OTel.Keys.Net;

/**
 * Deprecated, use `network.local.address`.
 * @stability development
 * @deprecated renamed → network.local.address
 */
#deprecated "renamed → network.local.address"
alias HostIp = "net.host.ip";

/**
 * Deprecated, use `server.address`.
 * @stability development
 * @deprecated renamed → server.address
 */
#deprecated "renamed → server.address"
alias HostName = "net.host.name";

/**
 * Deprecated, use `server.port`.
 * @stability development
 * @deprecated renamed → server.port
 */
#deprecated "renamed → server.port"
alias HostPort = "net.host.port";

/**
 * Deprecated, use `network.peer.address`.
 * @stability development
 * @deprecated renamed → network.peer.address
 */
#deprecated "renamed → network.peer.address"
alias PeerIp = "net.peer.ip";

/**
 * Deprecated, use `server.address` on client spans and `client.address` on server spans.
 * @stability development
 * @deprecated uncategorized: Replaced by `server.address` on client spans and `client.address` on server spans.
 */
#deprecated "uncategorized: Replaced by `server.address` on client spans and `client.address` on server spans."
alias PeerName = "net.peer.name";

/**
 * Deprecated, use `server.port` on client spans and `client.port` on server spans.
 * @stability development
 * @deprecated uncategorized: Replaced by `server.port` on client spans and `client.port` on server spans.
 */
#deprecated "uncategorized: Replaced by `server.port` on client spans and `client.port` on server spans."
alias PeerPort = "net.peer.port";

/**
 * Deprecated, use `network.protocol.name`.
 * @stability development
 * @deprecated renamed → network.protocol.name
 */
#deprecated "renamed → network.protocol.name"
alias ProtocolName = "net.protocol.name";

/**
 * Deprecated, use `network.protocol.version`.
 * @stability development
 * @deprecated renamed → network.protocol.version
 */
#deprecated "renamed → network.protocol.version"
alias ProtocolVersion = "net.protocol.version";

/**
 * Deprecated, use `network.transport` and `network.type`.
 * @stability development
 * @deprecated uncategorized: Split to `network.transport` and `network.type`.
 */
#deprecated "uncategorized: Split to `network.transport` and `network.type`."
alias SockFamily = "net.sock.family";

/**
 * Deprecated, use `network.local.address`.
 * @stability development
 * @deprecated renamed → network.local.address
 */
#deprecated "renamed → network.local.address"
alias SockHostAddr = "net.sock.host.addr";

/**
 * Deprecated, use `network.local.port`.
 * @stability development
 * @deprecated renamed → network.local.port
 */
#deprecated "renamed → network.local.port"
alias SockHostPort = "net.sock.host.port";

/**
 * Deprecated, use `network.peer.address`.
 * @stability development
 * @deprecated renamed → network.peer.address
 */
#deprecated "renamed → network.peer.address"
alias SockPeerAddr = "net.sock.peer.addr";

/**
 * Deprecated, no replacement at this time.
 * @stability development
 * @deprecated obsoleted: Removed. No replacement at this time.
 */
#deprecated "obsoleted: Removed. No replacement at this time."
alias SockPeerName = "net.sock.peer.name";

/**
 * Deprecated, use `network.peer.port`.
 * @stability development
 * @deprecated renamed → network.peer.port
 */
#deprecated "renamed → network.peer.port"
alias SockPeerPort = "net.sock.peer.port";

/**
 * Deprecated, use `network.transport`.
 * @stability development
 * @deprecated renamed → network.transport
 */
#deprecated "renamed → network.transport"
alias Transport = "net.transport";
