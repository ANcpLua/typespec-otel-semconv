// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "container" enum-typed attribute values. */
namespace OTel.Enums.Container;

/**
 * Deprecated, use `cpu.mode` instead.
 * @deprecated renamed → cpu.mode
 */
#deprecated "renamed → cpu.mode"
enum ContainerCpuState {
  /** When tasks of the cgroup are in user mode (Linux). When all container processes are in user mode (Windows). */
  User: "user",
  /** When CPU is used by the system (host OS) */
  System: "system",
  /** When tasks of the cgroup are in kernel mode (Linux). When all container processes are in kernel mode (Windows). */
  Kernel: "kernel",
}
