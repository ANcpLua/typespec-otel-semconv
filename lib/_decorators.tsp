// <auto-generated header — but contents are stable, edit if you change the marker decorator surface>
// Marker decorators consumed by the library's $onValidate checks.

namespace OTel;

/**
 * Marks a model as the consumer's TypeSpec implementation of an OpenTelemetry
 * entity (e.g. "service", "host", "k8s.pod"). The library's $onValidate
 * verifies the model carries every identifying attribute the upstream YAML
 * lists for that entity, by matching `@encodedName` defaults / property keys
 * against the upstream `role: identifying` ref set.
 *
 * @param name Upstream entity name string. Use OTel.Entities.<…>.<…>Entity for safety.
 */
extern dec otelEntity(target: Reflection.Model, name: valueof string);
