// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "v8js" enum-typed attribute values. */
namespace OTel.Enums.V8js;

/** The type of garbage collection. */
enum V8jsGcType {
  /** Major (Mark Sweep Compact). */
  Major: "major",
  /** Minor (Scavenge). */
  Minor: "minor",
  /** Incremental (Incremental Marking). */
  Incremental: "incremental",
  /** Weak Callbacks (Process Weak Callbacks). */
  Weakcb: "weakcb",
}

/** The name of the space type of heap memory. */
enum V8jsHeapSpaceName {
  /** New memory space. */
  NewSpace: "new_space",
  /** Old memory space. */
  OldSpace: "old_space",
  /** Code memory space. */
  CodeSpace: "code_space",
  /** Map memory space. */
  MapSpace: "map_space",
  /** Large object memory space. */
  LargeObjectSpace: "large_object_space",
}

/** The type of resource keeping the event loop active. */
enum V8jsResourceType {
  /** Active `setImmediate` callbacks. */
  Immediate: "Immediate",
  /** Active TCP Servers. */
  Tcpserverwrap: "TCPServerWrap",
  /** Active TCP connections. */
  Tcpwrap: "TCPWrap",
  /** Active `setTimeout` or `setInterval` timers. */
  Timeout: "Timeout",
  /** Active Terminal I/O (stdin/stdout). */
  Ttywrap: "TTYWrap",
}
