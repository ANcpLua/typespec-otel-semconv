// <auto-generated/>
// Source: open-telemetry/semantic-conventions @ v1.41.0
// Schema: https://opentelemetry.io/schemas/1.41.0
// Regenerate via: npm run generate

/** OpenTelemetry "aws" attribute keys (string constants matching the upstream YAML). */
namespace OTel.Keys.Aws;

/**
 * The unique identifier of the AWS Bedrock Guardrail. A [guardrail](https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails.html) helps safeguard and prevent unwanted behavior from model responses or user messages.
 * @stability development
 */
alias BedrockGuardrailId = "aws.bedrock.guardrail.id";

/**
 * The unique identifier of the AWS Bedrock Knowledge base. A [knowledge base](https://docs.aws.amazon.com/bedrock/latest/userguide/knowledge-base.html) is a bank of information that can be queried by models to generate more relevant responses and augment prompts.
 * @stability development
 */
alias BedrockKnowledgeBaseId = "aws.bedrock.knowledge_base.id";

/**
 * The JSON-serialized value of each item in the `AttributeDefinitions` request field.
 * @stability development
 */
alias DynamodbAttributeDefinitions = "aws.dynamodb.attribute_definitions";

/**
 * The value of the `AttributesToGet` request parameter.
 * @stability development
 */
alias DynamodbAttributesToGet = "aws.dynamodb.attributes_to_get";

/**
 * The value of the `ConsistentRead` request parameter.
 * @stability development
 */
alias DynamodbConsistentRead = "aws.dynamodb.consistent_read";

/**
 * The JSON-serialized value of each item in the `ConsumedCapacity` response field.
 * @stability development
 */
alias DynamodbConsumedCapacity = "aws.dynamodb.consumed_capacity";

/**
 * The value of the `Count` response parameter.
 * @stability development
 */
alias DynamodbCount = "aws.dynamodb.count";

/**
 * The value of the `ExclusiveStartTableName` request parameter.
 * @stability development
 */
alias DynamodbExclusiveStartTable = "aws.dynamodb.exclusive_start_table";

/**
 * The JSON-serialized value of each item in the `GlobalSecondaryIndexUpdates` request field.
 * @stability development
 */
alias DynamodbGlobalSecondaryIndexUpdates = "aws.dynamodb.global_secondary_index_updates";

/**
 * The JSON-serialized value of each item of the `GlobalSecondaryIndexes` request field
 * @stability development
 */
alias DynamodbGlobalSecondaryIndexes = "aws.dynamodb.global_secondary_indexes";

/**
 * The value of the `IndexName` request parameter.
 * @stability development
 */
alias DynamodbIndexName = "aws.dynamodb.index_name";

/**
 * The JSON-serialized value of the `ItemCollectionMetrics` response field.
 * @stability development
 */
alias DynamodbItemCollectionMetrics = "aws.dynamodb.item_collection_metrics";

/**
 * The value of the `Limit` request parameter.
 * @stability development
 */
alias DynamodbLimit = "aws.dynamodb.limit";

/**
 * The JSON-serialized value of each item of the `LocalSecondaryIndexes` request field.
 * @stability development
 */
alias DynamodbLocalSecondaryIndexes = "aws.dynamodb.local_secondary_indexes";

/**
 * The value of the `ProjectionExpression` request parameter.
 * @stability development
 */
alias DynamodbProjection = "aws.dynamodb.projection";

/**
 * The value of the `ProvisionedThroughput.ReadCapacityUnits` request parameter.
 * @stability development
 */
alias DynamodbProvisionedReadCapacity = "aws.dynamodb.provisioned_read_capacity";

/**
 * The value of the `ProvisionedThroughput.WriteCapacityUnits` request parameter.
 * @stability development
 */
alias DynamodbProvisionedWriteCapacity = "aws.dynamodb.provisioned_write_capacity";

/**
 * The value of the `ScanIndexForward` request parameter.
 * @stability development
 */
alias DynamodbScanForward = "aws.dynamodb.scan_forward";

/**
 * The value of the `ScannedCount` response parameter.
 * @stability development
 */
alias DynamodbScannedCount = "aws.dynamodb.scanned_count";

/**
 * The value of the `Segment` request parameter.
 * @stability development
 */
alias DynamodbSegment = "aws.dynamodb.segment";

/**
 * The value of the `Select` request parameter.
 * @stability development
 */
alias DynamodbSelect = "aws.dynamodb.select";

/**
 * The number of items in the `TableNames` response parameter.
 * @stability development
 */
alias DynamodbTableCount = "aws.dynamodb.table_count";

/**
 * The keys in the `RequestItems` object field.
 * @stability development
 */
alias DynamodbTableNames = "aws.dynamodb.table_names";

/**
 * The value of the `TotalSegments` request parameter.
 * @stability development
 */
alias DynamodbTotalSegments = "aws.dynamodb.total_segments";

/**
 * The ARN of an [ECS cluster](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/clusters.html).
 * @stability development
 */
alias EcsClusterArn = "aws.ecs.cluster.arn";

/**
 * The Amazon Resource Name (ARN) of an [ECS container instance](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ECS_instances.html).
 * @stability development
 */
alias EcsContainerArn = "aws.ecs.container.arn";

/**
 * The [launch type](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html) for an ECS task.
 * @stability development
 */
alias EcsLaunchtype = "aws.ecs.launchtype";

/**
 * The ARN of a running [ECS task](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-account-settings.html#ecs-resource-ids).
 * @stability development
 */
alias EcsTaskArn = "aws.ecs.task.arn";

/**
 * The family name of the [ECS task definition](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definitions.html) used to create the ECS task.
 * @stability development
 */
alias EcsTaskFamily = "aws.ecs.task.family";

/**
 * The ID of a running ECS task. The ID MUST be extracted from `task.arn`.
 * @stability development
 */
alias EcsTaskId = "aws.ecs.task.id";

/**
 * The revision for the task definition used to create the ECS task.
 * @stability development
 */
alias EcsTaskRevision = "aws.ecs.task.revision";

/**
 * The ARN of an EKS cluster.
 * @stability development
 */
alias EksClusterArn = "aws.eks.cluster.arn";

/**
 * The AWS extended request ID as returned in the response header `x-amz-id-2`.
 * @stability development
 */
alias ExtendedRequestId = "aws.extended_request_id";

/**
 * The name of the AWS Kinesis [stream](https://docs.aws.amazon.com/streams/latest/dev/introduction.html) the request refers to. Corresponds to the `--stream-name` parameter of the Kinesis [describe-stream](https://docs.aws.amazon.com/cli/latest/reference/kinesis/describe-stream.html) operation.
 * @stability development
 */
alias KinesisStreamName = "aws.kinesis.stream_name";

/**
 * The full invoked ARN as provided on the `Context` passed to the function (`Lambda-Runtime-Invoked-Function-Arn` header on the `/runtime/invocation/next` applicable).
 * @stability development
 */
alias LambdaInvokedArn = "aws.lambda.invoked_arn";

/**
 * The UUID of the [AWS Lambda EvenSource Mapping](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-lambda-eventsourcemapping.html). An event source is mapped to a lambda function. It's contents are read by Lambda and used to trigger a function. This isn't available in the lambda execution context or the lambda runtime environment. This is going to be populated by the AWS SDK for each language when that UUID is present. Some of these operations are Create/Delete/Get/List/Update EventSourceMapping.
 * @stability development
 */
alias LambdaResourceMappingId = "aws.lambda.resource_mapping.id";

/**
 * The Amazon Resource Name(s) (ARN) of the AWS log group(s).
 * @stability development
 */
alias LogGroupArns = "aws.log.group.arns";

/**
 * The name(s) of the AWS log group(s) an application is writing to.
 * @stability development
 */
alias LogGroupNames = "aws.log.group.names";

/**
 * The ARN(s) of the AWS log stream(s).
 * @stability development
 */
alias LogStreamArns = "aws.log.stream.arns";

/**
 * The name(s) of the AWS log stream(s) an application is writing to.
 * @stability development
 */
alias LogStreamNames = "aws.log.stream.names";

/**
 * The AWS request ID as returned in the response headers `x-amzn-requestid`, `x-amzn-request-id` or `x-amz-request-id`.
 * @stability development
 */
alias RequestId = "aws.request_id";

/**
 * The S3 bucket name the request refers to. Corresponds to the `--bucket` parameter of the [S3 API](https://docs.aws.amazon.com/cli/latest/reference/s3api/index.html) operations.
 * @stability development
 */
alias S3Bucket = "aws.s3.bucket";

/**
 * The source object (in the form `bucket`/`key`) for the copy operation.
 * @stability development
 */
alias S3CopySource = "aws.s3.copy_source";

/**
 * The delete request container that specifies the objects to be deleted.
 * @stability development
 */
alias S3Delete = "aws.s3.delete";

/**
 * The S3 object key the request refers to. Corresponds to the `--key` parameter of the [S3 API](https://docs.aws.amazon.com/cli/latest/reference/s3api/index.html) operations.
 * @stability development
 */
alias S3Key = "aws.s3.key";

/**
 * The part number of the part being uploaded in a multipart-upload operation. This is a positive integer between 1 and 10,000.
 * @stability development
 */
alias S3PartNumber = "aws.s3.part_number";

/**
 * Upload ID that identifies the multipart upload.
 * @stability development
 */
alias S3UploadId = "aws.s3.upload_id";

/**
 * The ARN of the Secret stored in the Secrets Manager
 * @stability development
 */
alias SecretsmanagerSecretArn = "aws.secretsmanager.secret.arn";

/**
 * The ARN of the AWS SNS Topic. An Amazon SNS [topic](https://docs.aws.amazon.com/sns/latest/dg/sns-create-topic.html) is a logical access point that acts as a communication channel.
 * @stability development
 */
alias SnsTopicArn = "aws.sns.topic.arn";

/**
 * The URL of the AWS SQS Queue. It's a unique identifier for a queue in Amazon Simple Queue Service (SQS) and is used to access the queue and perform actions on it.
 * @stability development
 */
alias SqsQueueUrl = "aws.sqs.queue.url";

/**
 * The ARN of the AWS Step Functions Activity.
 * @stability development
 */
alias StepFunctionsActivityArn = "aws.step_functions.activity.arn";

/**
 * The ARN of the AWS Step Functions State Machine.
 * @stability development
 */
alias StepFunctionsStateMachineArn = "aws.step_functions.state_machine.arn";
